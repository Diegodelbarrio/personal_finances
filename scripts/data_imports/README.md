# Private data imports

Real financial CSV files must not be stored in this directory or committed to Git.

Keep personal imports under `private_data/data_imports/` (ignored by Git) and pass
their path explicitly to the relevant management command or upload them through
the authenticated application.

Only fully synthetic examples may be added to the repository.
